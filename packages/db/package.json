{
  "name": "@packages/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/kysely.ts",
  "scripts": {
    "build": "tsc",
    "migrate": "pnpm build && node dist/migrator.js && pnpm generate-types",
    "migrate:prod": "node dist/migrator.js",
    "generate-types": "tsx src/generate-types.ts",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "kysely": "^0.27.4",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/pg": "^8.15.6",
    "kysely-codegen": "^0.17.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
