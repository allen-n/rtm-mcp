{
  "name": "@packages/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/kysely.ts",
  "scripts": {
    "migrate": "tsx src/migrator.ts",
    "generate": "kysely-codegen --out-file=src/generated-types.ts",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "kysely": "^0.27.4"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.10.5",
    "kysely-codegen": "^0.17.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
