# Railway configuration for RTM MCP Server
# Deploy as a monorepo with two services: web and mcp-server

[build]
builder = "nixpacks"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
